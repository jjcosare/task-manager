# TODO use @Value test on DataSourceContainer.java, currently results to null when used
docker.datasource.version=postgres:15
spring.datasource.driver-class-name=${DATASOURCE_DRIVER}
spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USERNAME}
spring.datasource.password=${DATASOURCE_PASSWORD}

task.random.creation.enabled=false
task.random.creation.min=3
task.random.creation.max=15